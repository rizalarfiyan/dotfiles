# dotfiles — rizalarfiyan


Installation code:
```
sudo pacman -Syu stow zsh git curl wget base-devel
sudo pacman -Syu fzf zoxide ripgrep fd bat eza jq tldr tmux direnv gping
sudo pacman -Syu fastfetch btop lazygit
```
